package com.vinhdp.abstraction.multi;

/** Một CLASS thường, cũng có send(). Class luôn thắng interface. */
public class BaseNotifier {

    public String send() {
        return "BaseNotifier.send()";
    }
}
