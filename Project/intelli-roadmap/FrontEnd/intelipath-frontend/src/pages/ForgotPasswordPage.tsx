// "Forgot password" moved into the sign-in popup — see
// features/shared/auth/components/LoginDialog.tsx (a view swap, no navigation).
// The /forgot-password route now redirects home (see AppRoutes).

export {}
