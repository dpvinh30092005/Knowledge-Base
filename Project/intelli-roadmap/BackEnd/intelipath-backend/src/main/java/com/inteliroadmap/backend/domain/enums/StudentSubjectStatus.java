package com.inteliroadmap.backend.domain.enums;

/** How far a student is with an FPT subject. v1 only uses PASSED; the rest are reserved. */
public enum StudentSubjectStatus {
    PASSED,
    IN_PROGRESS,
    PLANNED
}
